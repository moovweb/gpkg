package pkg

import "testing"

//import . "source"

func TestPackage(t *testing.T) {
/*	pkg_path := "/home/jbussdieker/moovweb/gpkg/pkg/testdata"
	p := NewPackage("simple_cmd", nil, pkg_path, NewLocalSource(pkg_path))
	err := p.Clone("/tmp/gpkg-test/package-test")
	if err != nil {
		t.Fatal(err)
	}
	_, err = p.Clean()
	if err != nil {
		t.Fatal(err)
	}
	_, err = p.Build()
	if err != nil {
		t.Fatal(err)
	}*/
}
